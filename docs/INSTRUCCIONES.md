# 📋 Guía Rápida de Instalación y Uso

Si acabas de recibir el archivo **NeronCentralita.exe**, sigue estos pasos para poner en marcha la centralita.

## 🚀 Instalación
1. Asegúrate de que el archivo `NeronCentralita.exe` y `node_sqlite3.node` están en la **misma carpeta**.
2. Haz doble clic en `NeronCentralita.exe`.
3. Si Windows te pide permiso para la red (Firewall), dale a **Permitir acceso**. Es necesario para que otros ordenadores se conecten a tu servidor.

## 💻 Acceso
- **En el mismo ordenador**: Abre el navegador y ve a `http://localhost:3000`.
- **Desde otro ordenador de la oficina**: Pon la IP del ordenador servidor y el puerto, por ejemplo: `http://192.168.1.50:3000`.

## 🔑 Cuentas por Defecto
- **Usuario**: `admin`
- **Contraseña**: `neron`
*(Se recomienda cambiar la contraseña o crear nuevos usuarios admin nada más entrar)*.

## 📂 ¿Dónde están mis datos?
Toda la información se guarda en la carpeta **BaseCentralita**. No la borres a menos que quieras empezar de cero.
